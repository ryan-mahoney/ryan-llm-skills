interface:
  display_name: "Spec Work Tour"
  short_description: "Review context, proof, and separate release states"
  default_prompt: "Use $spec-work-tour to build the final executable-evidence tour for this implemented spec."
